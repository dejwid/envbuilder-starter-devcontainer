{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/michidk/devcontainers-features/bun:1.0.1": {}
  },
  "postCreateCommand": "node --version && npm --version && bun --version"
}
